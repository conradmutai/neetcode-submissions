class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        checked = set()
        for num in nums:
            if num in checked:
                True
            seen.add(num)
        return False

